<?php
$product = $block->getCurrentProduct();
if ($product) {
    $sku = $product->getSku();
    $count = $block->getAddToCartCount($sku);
} else {
    $sku = null;
    $count = null;
    echo "Product is null!";
}
?>
<?php if ($count > 0): ?>
    <div class="hiphop-counter">
        This HipHop product has been added to cart <?= $count ?> times.
    </div>
<?php endif; ?>
